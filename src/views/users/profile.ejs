<!DOCTYPE html>
<html lang="es">
    <%- include('./partials/head') %>
    <title>Perfil</title>
    <link rel="stylesheet" href="/css/profile.css">
    <body>
        <%- include('./partials/header') %>
        <main>
            <div class="user-info">
                <h1>Bienvenido otra vez, <%= user.first_name %></h1>
                <figure><img src="/images/users/<%= user.image %>" alt="profile-picture"></figure>
            </div>
            <div class="user-options">
                <form action="/user/logout" method="POST">
                    <button type="submit">Cerrar sesión</button>
                </form>
                <% if(user.category == 'admin'){ %>
                    <a href="/user/panel">Administrar</a>
                <% } %>
            </div>
        </main>
        <%- include('./partials/footer') %>
    </body>
</html>